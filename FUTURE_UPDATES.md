# Future Updates

## Android polish

- Regenerate the Android launch/splash assets so the startup flash matches the current Inner Circle icon. The launcher icon shown on the home screen is correct, but Android can briefly show older launch assets from `android/app/src/main/res/drawable/splash.png`, `android/app/src/main/res/drawable-v24/ic_launcher_foreground.xml`, or the adaptive icon foreground files under `android/app/src/main/res/mipmap-*` during app startup. This is cosmetic and can wait for the next planned app update.
